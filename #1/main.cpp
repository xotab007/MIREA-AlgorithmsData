#include <iostream>

using namespace std;

struct Stack {
    private:
        int value;
        int head = -1; //Крайний элемент
        struct stack *next; //Указывает на следующий элемент

    public:
        Stack(){
            
        }
};

int pop(stak *next){
    if (lengh == 0){

    }
};

int main() {
    List *NewList = new List;

    Show(NewList);
    return 0;
}